{
  "name": "meeting-n8n-aws",
  "version": "1.0.0",
  "private": true,
  "description": "AWS/VPS deployment package for the Meeting Notes n8n workflow",
  "scripts": {
    "up": "docker compose --env-file .env up -d --build",
    "down": "docker compose down",
    "logs": "docker compose logs -f n8n",
    "import:workflow": "docker compose exec n8n n8n import:workflow --input=/workflows/meeting-notes-main-pipeline.json",
    "validate:compose": "docker compose config"
  }
}

